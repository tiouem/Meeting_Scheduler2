<Page
    x:Class="Meeting_Scheduler_App.View.MeetingDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Meeting_Scheduler_App.ViewModel"
    xmlns:local="using:Meeting_Scheduler_App.View"
    xmlns:common="using:Meeting_Scheduler_App.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" RequestedTheme="Light">

    <Page.DataContext>
        <vm:MeetingDetail_VM></vm:MeetingDetail_VM>
    </Page.DataContext>
    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="6*"/>
            </Grid.ColumnDefinitions>
            <Button RequestedTheme="Light" Margin="39,59,39,0"
                        Style="{StaticResource NavigationBackButtonNormalStyle}" Command="{Binding GoBackClick}"/>
            <TextBlock RequestedTheme="Light" Text="{Binding Meeting.Name}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Grid.Row="1">
                <TextBlock RequestedTheme="Light" FontWeight="SemiBold" FontSize="15">Description:</TextBlock>
                <TextBlock Text="{Binding Meeting.Description}" RequestedTheme="Light" FontSize="15" TextWrapping="Wrap" FontFamily="Segoe UI" Margin="0,0,0,20"></TextBlock>
                <TextBlock RequestedTheme="Light" FontWeight="SemiBold" FontSize="15">Room:</TextBlock>
                <TextBlock Text="{Binding Meeting.Room.Type}" RequestedTheme="Light"  FontSize="15" FontFamily="Segoe UI" Margin="0,0,0,20"></TextBlock>
                <TextBlock Text="{Binding Date}" RequestedTheme="Light"  FontSize="15" FontFamily="Segoe UI" FontWeight="SemiBold" Margin="0,0,0,20"></TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
    <Page.BottomAppBar>
        <AppBar>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <AppBarButton Icon="Edit" Command="{Binding Edit}" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal"/>
            </Grid>
        </AppBar>
    </Page.BottomAppBar>
</Page>

